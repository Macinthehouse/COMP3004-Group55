#ifndef VENDORCATEGORY_H
#define VENDORCATEGORY_H

// VendorCategory Enum

// Represents the type of vendor in the system.
// Used for:
// - Stall capacity rules
// - Waitlist separation

enum class VendorCategory {
    FOOD,
    ARTISAN
};

#endif // VENDORCATEGORY_H
